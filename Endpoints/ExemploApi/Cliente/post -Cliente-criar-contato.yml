info:
  name: post /Cliente/criar/contato
  type: http
  seq: 8
  tags:
    - Cliente

http:
  method: POST
  url: "{{baseUrl}}/Cliente/criar/contato"
  params:
    - name: cpf
      value: ""
      type: query
      disabled: true
  body:
    type: json
    data: |-
      {
        "tipoContato": "",
        "ddd": 0,
        "telefone": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/Cliente/criar/contato"
      method: POST
      params:
        - name: cpf
          value: ""
          type: query
          disabled: true
      body:
        type: json
        data: |-
          {
            "tipoContato": "",
            "ddd": 0,
            "telefone": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
