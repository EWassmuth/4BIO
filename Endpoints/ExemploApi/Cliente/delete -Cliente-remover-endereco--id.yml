info:
  name: delete /Cliente/remover/endereco/:id
  type: http
  seq: 7
  tags:
    - Cliente

http:
  method: DELETE
  url: "{{baseUrl}}/Cliente/remover/endereco/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/Cliente/remover/endereco/:id"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
